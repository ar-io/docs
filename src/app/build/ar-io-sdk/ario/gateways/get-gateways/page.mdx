import {CodeGroup} from "@/components/Code"
import {HeroPattern} from "@/components/HeroPattern"

<HeroPattern />

# getGateways

`getGateways` is a method on the `ARIO` class that retrieves registered gateways from the ar.io Network. Results are paginated, and can be sorted by any attribute of the gateway object.

`getGateways` does not require authentication.

## Parameters

{
<div style={{textAlign: "center"}}>
    <table className="inline-table">
    <thead>
    <tr>
        <th>Parameter</th>
        <th>Type</th>
        <th>Description</th>
        <th>Optional</th>
        <th>Default</th>
    </tr>
    </thead>
    <tbody>
    <tr>
        <th>cursor</th>
        <td>String</td>
        <td>Cursor used for paginated results</td>
        <td>true</td>
        <td>None</td>
    </tr>
    <tr>
        <th>limit</th>
        <td>Number</td>
        <td>Max number of results to return in a single search</td>
        <td>true</td>
        <td>100</td>
    </tr>
    <tr>
        <th>sortBy</th>
        <td>String</td>
        <td>Property used to sort returned results</td>
        <td>true</td>
        <td>startTimestamp</td>
    </tr>
    <tr>
        <th>sortOrder</th>
        <td>String</td>
        <td>Order in which sorted results are returned. valid values are `desc` or `asc`</td>
        <td>true</td>
        <td>desc</td>
    </tr>
    </tbody>
    </table>
</div>
}

## Example

<CodeGroup title="getGateways">

    ```javascript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.init();
      const gateway = await ario.getGateway({
        address: 'f_EcJ8tYOFYjBMCIi-dlmzg8AiWZ2hELrXRFWPKCv9I',
      });

      console.log(gateway);
    }

    main();
    ```

    ```javascript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.init();
      const gateway = await ario.getGateway({
        address: 'f_EcJ8tYOFYjBMCIi-dlmzg8AiWZ2hELrXRFWPKCv9I',
      });

      console.log(gateway);
    }

    main();
    ```

</CodeGroup>


## Output

```typescript
{
  nextCursor: 'L-CYI5VllWl24siIbjc213PdCjYm0S5k0q-oY4ynIhE',
  limit: 3,
  sortBy: 'operatorStake',
  items: [
    {
      vaults: [Object],
      endTimestamp: 1733903070665,
      settings: [Object],
      weights: [Object],
      stats: [Object],
      observerAddress: '9jfM0uzGNc9Mkhjo1ixGoqM7ygSem9wx_EokiVgi0Bs',
      operatorStake: 0,
      startTimestamp: 1719295200000,
      status: 'leaving',
      gatewayAddress: '9jfM0uzGNc9Mkhjo1ixGoqM7ygSem9wx_EokiVgi0Bs',
      totalDelegatedStake: 0,
      delegates: []
    },
    {
      status: 'leaving',
      operatorStake: 0,
      endTimestamp: 1733903070665,
      totalDelegatedStake: 0,
      stats: [Object],
      observerAddress: '4nPcBLmadsIson8CAIbotO1UH5PphO5blWrKLivA-6o',
      settings: [Object],
      startTimestamp: 1719295200000,
      vaults: [Object],
      gatewayAddress: '4H1pwtd0GozlzIvmaIgWd-f8QOZ30mjdzyuYmxbLPzY',
      weights: [Object],
      delegates: []
    },
    {
      status: 'leaving',
      operatorStake: 0,
      endTimestamp: 1734126270631,
      weights: [Object],
      stats: [Object],
      observerAddress: 'L-CYI5VllWl24siIbjc213PdCjYm0S5k0q-oY4ynIhE',
      settings: [Object],
      startTimestamp: 1719295200000,
      vaults: [Object],
      gatewayAddress: 'L-CYI5VllWl24siIbjc213PdCjYm0S5k0q-oY4ynIhE',
      totalDelegatedStake: 0,
      delegates: []
    }
  ],
  totalItems: 301,
  sortOrder: 'asc',
  hasMore: true
}
```
