import {CodeGroup} from "@/components/Code"
import {HeroPattern} from "@/components/HeroPattern"
import { Table } from '@/components/Table'

<HeroPattern />

# getTokenCost

`getTokenCost` is a method on the `ARIO` class that calculates the cost in mARIO tokens for a specific interaction (such as buying an ArNS record). The cost is calculated based on the interaction type and its parameters.

`getTokenCost` does not require authentication.

## Parameters

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Header>Parameter</Table.Header>
      <Table.Header>Type</Table.Header>
      <Table.Header>Description</Table.Header>
      <Table.Header>Optional</Table.Header>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-semibold">intent</Table.Cell>
      <Table.Cell>string</Table.Cell>
      <Table.Cell>The type of interaction to calculate the cost for (e.g., 'Buy-Record')</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-semibold">args</Table.Cell>
      <Table.Cell>object</Table.Cell>
      <Table.Cell>The specific parameters required for the interaction type</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

## Examples

<CodeGroup title="getTokenCost">

    ```typescript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.init();
      const price = await ario
        .getTokenCost({
            intent: 'Buy-Record',
            name: 'ar-io',
            type: 'permabuy',
        })
        .then((p) => new mIOToken(p).toIO()); // convert to IO tokens for readability
      
      console.log('Cost in IO tokens:', price);
    }

    main();
    ```

    ```typescript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.init();
      const price = await ario
        .getTokenCost({
            intent: 'Buy-Record',
            name: 'ar-io',
            type: 'permabuy',
        })
        .then((p) => new mIOToken(p).toIO()); // convert to IO tokens for readability
      
      console.log('Cost in IO tokens:', price);
    }

    main();
    ```

</CodeGroup>

## Output

```js
1642.34  // Cost in IO tokens (converted from mARIO for readability)
```