import { CodeGroup } from '@/components/Code'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'getArNSRecord',
  description: 'Retrieve details for a specific ArNS record by name',
  openGraph: {
    title: 'getArNSRecord',
  },
}

<HeroPattern />

# getArNSRecord

`getArNSRecord` is a method on the `ARIO` class that retrieves the details of a specific ArNS record by its name. This includes lease information, type, process ID, and other metadata.

`getArNSRecord` does not require authentication.

## Parameters

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Header>Parameter</Table.Header>
      <Table.Header>Type</Table.Header>
      <Table.Header>Description</Table.Header>
      <Table.Header>Optional</Table.Header>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-semibold">name</Table.Cell>
      <Table.Cell>string</Table.Cell>
      <Table.Cell>The ArNS name to retrieve details for</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

## Examples

<CodeGroup title="getArNSRecord">

    ```typescript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.init();
      const arnsRecord = await ario.getArNSRecord({ name: 'ardrive' });

      console.log(arnsRecord);
    }

    main();
    ```

    ```typescript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.init();
      const arnsRecord = await ario.getArNSRecord({ name: 'ardrive' });

      console.log(arnsRecord);
    }

    main();
    ```

</CodeGroup>

## Output

```js
{
  "processId": "bh9l1cy0aksiL_x9M359faGzM_yjralacHIUo8_nQXM",
  "startTimestamp": 1707307200000,
  "endTimestamp": 1739307200000,
  "type": "lease",
  "undernameLimit": 10,
  "purchasePrice": 75541282,
  "renewalPrice": 75541282
}
```
