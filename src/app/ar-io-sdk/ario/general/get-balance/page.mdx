import { CodeGroup } from '@/components/Code'

export const metadata = {
  title: 'getBalance',
  description: 'Retrieve the token balance for a specific wallet address',
  openGraph: {
    title: 'getBalance',
  },
}


# getBalance

`getBalance` is a method on the `ARIO` class that retrieves the token balance for a specific wallet address.

`getBalance` does not require authentication.

## Parameters

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Header>Parameter</Table.Header>
      <Table.Header>Type</Table.Header>
      <Table.Header>Description</Table.Header>
      <Table.Header>Optional</Table.Header>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-semibold">address</Table.Cell>
      <Table.Cell>string - WalletAddress</Table.Cell>
      <Table.Cell>The wallet address to retrieve the balance for</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

## Examples

<CodeGroup title="getBalance">

    ```typescript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.init();
      const balance = await ario.getBalance({
        address: 't4Xr0_J4Iurt7caNST02cMotaz2FIbWQ4Kbj616RHl3'
      });

      console.log(balance);
    }

    main();
    ```

    ```typescript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.init();
      const balance = await ario.getBalance({
        address: 't4Xr0_J4Iurt7caNST02cMotaz2FIbWQ4Kbj616RHl3'
      });

      console.log(balance);
    }

    main();
    ```

</CodeGroup>

## Output

```js
1234567890
```
