import { CodeGroup } from '@/components/Code'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'Logging',
  description: 'Logging configuration and options for the AR.IO SDK',
  openGraph: {
    title: 'Logging',
  },
}

<HeroPattern />

# Logging
