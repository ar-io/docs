import { CodeGroup } from '@/components/Code'
import { Table } from '@/components/Table'

export const metadata = {
  title: 'claimWithCaptchaResponse',
  description: 'Claim tokens directly with captcha response',
  openGraph: {
    title: 'claimWithCaptchaResponse',
  },
}

# claimWithCaptchaResponse

`claimWithCaptchaResponse` is a method on the `faucet` object that claims tokens directly using a captcha response. This method combines captcha verification and token claiming in a single step.

`claimWithCaptchaResponse` does not require authentication.

## Examples

<CodeGroup title="claimWithCaptchaResponse">

    ```typescript {{title: "Basic"}}
    const result = await ario.faucet.claimWithCaptchaResponse({
        captchaResponse: 'captcha_response_token_here',
        recipient: '7oqF5r...LbpfT7',
        quantity: 1000000000
    });
    ```

    ```typescript {{ title: 'NodeJS' }}
    const { ARIO } = require('@ar.io/sdk');

    async function main() {
      const ario = ARIO.testnet(); // faucetUrl defaults to 'https://faucet.ario.permaweb.services'

      const result = await ario.faucet.claimWithCaptchaResponse({
        captchaResponse: 'captcha_response_token_here',
        recipient: '7oqF5r...LbpfT7',
        quantity: 1000000000
      });

      console.log('Claim ID:', result.id);
      console.log('Success:', result.success);
    }

    main();
    ```

    ```typescript {{ title: 'Web' }}
    import { ARIO } from '@ar.io/sdk/web';

    async function main() {
      const ario = ARIO.testnet(); // faucetUrl defaults to 'https://faucet.ario.permaweb.services'

      const result = await ario.faucet.claimWithCaptchaResponse({
        captchaResponse: 'captcha_response_token_here',
        recipient: '7oqF5r...LbpfT7',
        quantity: 1000000000
      });

      console.log('Claim ID:', result.id);
      console.log('Success:', result.success);
    }

    main();
    ```

</CodeGroup>

## Parameters

<Table>
  <Table.Head>
    <Table.Row>
      <Table.Header>Parameter</Table.Header>
      <Table.Header>Type</Table.Header>
      <Table.Header>Description</Table.Header>
      <Table.Header>Optional</Table.Header>
    </Table.Row>
  </Table.Head>
  <Table.Body>
    <Table.Row>
      <Table.Cell className="font-semibold">captchaResponse</Table.Cell>
      <Table.Cell>string</Table.Cell>
      <Table.Cell>
        The captcha response from solving the captcha challenge
      </Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-semibold">recipient</Table.Cell>
      <Table.Cell>string</Table.Cell>
      <Table.Cell>The wallet address to receive the claimed tokens</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
    <Table.Row>
      <Table.Cell className="font-semibold">quantity</Table.Cell>
      <Table.Cell>number</Table.Cell>
      <Table.Cell>The quantity of tokens to claim</Table.Cell>
      <Table.Cell>false</Table.Cell>
    </Table.Row>
  </Table.Body>
</Table>

## Output

```js
{
  "id": "claim_123456789",
  "success": true
}
```
